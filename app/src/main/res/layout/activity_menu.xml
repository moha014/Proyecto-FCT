<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/logo1"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="16dp">

    <!-- Logo como toolbar decorativo -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@drawable/iberdrolalogo"
        android:gravity="center"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Espacio entre toolbar y botones -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="60dp" />

    <!-- Botones con diseño moderno y atractivo -->
    <Button
        android:id="@+id/btnFacturas"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:backgroundTint="#4CAF50"
        android:elevation="8dp"
        android:padding="18dp"
        android:text="📋 Facturas"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold"
        android:alpha="1"
        app:cornerRadius="24dp" />

    <Button
        android:id="@+id/btnSmartSolar"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:backgroundTint="#66BB6A"
        android:elevation="8dp"
        android:padding="18dp"
        android:text="☀️ Smart Solar"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold"
        android:alpha="1"
        app:cornerRadius="24dp" />

    <!-- Botón de Mock claramente distinto -->
    <Button
        android:id="@+id/btnMockToggle"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:backgroundTint="#FF9800"
    android:elevation="12dp"
    android:padding="18dp"
    android:text="🔄 Alternar Mock"
    android:textColor="#FFFFFF"
    android:textSize="20sp"
    android:textStyle="bold"
    android:alpha="0.7"
    app:cornerRadius="24dp" />
</LinearLayout>
